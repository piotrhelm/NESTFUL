from typing import List



def split_words(s: str) -> List[str]:

    """Splits a text string `s` into words, where words are separated by one or more white spaces (spaces, tabs, or newlines).

    Args:

        s: The input string to be split into words.

    Returns:

        A list of words (strings).

    """

    return s.split()

