from typing import Tuple



def is_greater_than(a: int, b: int) -> bool:

    """

    Returns `True` if `a` is greater than `b`, and `False` otherwise.

    Args:

        a: The first integer to compare.

        b: The second integer to compare.

    """

    return a > b  # Inline comment to explain the comparison operation

