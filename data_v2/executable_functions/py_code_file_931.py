from typing import Any



def number_of_gates(n: int) -> int:

    """Calculates the number of AND gates needed to implement a full adder circuit for two `n`-bit numbers.

    Args:

        n: The number of bits in the input numbers.

    Returns:

        The number of AND gates needed to implement the full adder circuit.

    """

    return 2 + 3 + 2

